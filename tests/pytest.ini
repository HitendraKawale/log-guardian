[pytest]
# Only contract/ runs by default - it needs nothing. integration/ talks to the
# Compose stack and is opted into explicitly: `pytest integration`.
testpaths = contract
pythonpath = ..
markers =
    integration: requires the Docker Compose stack (make up)
