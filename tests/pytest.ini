[pytest]
# Only contract/ runs by default - it needs nothing. The other two talk to the
# Compose stack and are opted into explicitly: `pytest integration`, `pytest e2e`.
testpaths = contract
pythonpath = ..
markers =
    integration: requires the Docker Compose stack (make up)
    e2e: drives a real browser against the stack (make up)
